Package.Json (they made a change to a new package. updated docs below).

"babel-core": "6.26.0",
"babel-loader": "7.1.2",
"babel-preset-es2015": "6.24.1"



webpack.config.js (The module rules is what needs to be added)

"use strict";

module.exports = {
    entry: {
        WebPackPage: "./wwwroot/js/Pages/WebPagePageUI.js"
    },
    output: {
        //when just using web pack
        //filename: "./wwwroot/js/Distribution/[name]Bundle.js"

        //use this when we run from gulp because gulp outputs with full path
        filename: '[name]Bundle.js'
    },
       module: {
         loaders: [
             {
                 test: /\.js$/,
                 loader: 'babel-loader?presets[]=es2015'
             }
         ]
     }
};
